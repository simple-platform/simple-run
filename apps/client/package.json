{
  "name": "@simple-run/client",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "rm -rf burrito_out && rm -rf ../../_build/dev/rel/desktop && mix ex_tauri dev",
    "build": "mix compile --force",
    "test": "MIX_ENV=test mix coveralls.html"
  }
}
